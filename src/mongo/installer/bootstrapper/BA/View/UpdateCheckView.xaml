<UserControl x:Class="MongoDB.Bootstrapper.BA.View.UpdateCheckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="341" d:DesignWidth="634">

    <UserControl.Resources>
        <ResourceDictionary Source="Styles.xaml"/>
    </UserControl.Resources>

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ProgressBar Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="0,56,24,0" Value="{Binding UpdateCheckViewModel.CheckProgress}" Height="10" />

        <!-- Skip -->
        <Button Grid.Row="2" IsCancel="True" IsDefault="True" HorizontalAlignment="Right" Command="NavigationCommands.NextPage" Content="Skip"/>
    </Grid>
</UserControl>
